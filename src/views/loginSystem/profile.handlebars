<h2>USER PROFILE</h2>
<section class="userProfileData">
    {{#if user.last_name}} <p><strong>Lastname: </strong>{{user.last_name}}</p>{{/if}}
    <p><strong>Age: </strong>{{user.age}}</p>
    {{#if user.email}} <p><strong>Email: </strong>{{user.email}}</p> {{/if}}
    <p><strong>Role: </strong>{{user.role}}</p>
    <div class="profileOptions">
        <a href="/logout" title="Logout"><img src="/img/logout.png" alt="Icono para cierre de sesión" class="logoutImg"></a>
        <button class="button redButton" data-userid="{{user.userId}}">Delete account</button>
    </div>
</section>

<script src="/js/loginSystem/profile.js"></script>