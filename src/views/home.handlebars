<header class="navBar">
    <a href="/profile"><img src="/img/usuario.png" alt="Imagen de perfil" class="navImg"></a>
    <div>
        <p>Welcome {{user.first_name}} {{#if user.last_name}}{{user.last_name}}{{/if}}</p>
        {{#if user.email}} <p>Email: {{user.email}}</p> {{/if}}
        <p>Role: {{user.role}}</p>
    </div>
    <a href="/logout"><img src="/img/logout.png" alt="Icono para cierre de sesión" class="navImg"></a>
</header>

<section class="homeSection">
    <h1>SHOP EASY - CODERHOUSE</h1>
    <img src="/img/grow-shop.png" alt="Descripción de la imagen">
    <div>
        {{#if roles.adminRole}}
        <a href="/realtimeproducts" class="bold">🔗 Inventory</a>
        <a href="/users" class="bold">🔗 Users</a>
        {{/if}}
        {{#if roles.premiumRole}}
        <a href="/realtimeproducts" class="bold">🔗 Inventory</a>
        <a href="/chat" class="bold">🔗 Chat</a>
        <a href="/products" class="bold">🔗 View Products</a>
        {{/if}}
        {{#if roles.userRole}}
        <a href="/chat" class="bold">🔗 Chat</a>
        <a href="/products" class="bold">🔗 View Products</a>
        {{/if}}
    </div>
</section>